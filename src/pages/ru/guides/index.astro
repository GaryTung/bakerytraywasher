---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import BreadcrumbNav from '../../../components/BreadcrumbNav.astro';
import CTASection from '../../../components/CTASection.astro';
import { guides, guidesHub } from '../../../i18n/guides';
import { t } from '../../../i18n/utils';

const locale = 'ru' as const;
const hub = guidesHub[locale];
const readLabel = "Читать руководство";
---
<BaseLayout
  title={"Руководства JD-3 — Машина для мойки противней"}
  description={"9 практических руководств по выбору, установке и эксплуатации коммерческой машины для мойки противней."}
  locale={locale}
  canonicalPath="/guides/"
  schemaType="Article"
>
<BreadcrumbNav items={[{label: t(locale, 'nav.home'), href: '/ru/'}, {label: hub.h1}]} />

<section class="section">
  <div class="container-page">
    <h1 class="text-center mb-4">{hub.h1}</h1>
    <p class="text-bakery-navy/70 text-lg max-w-2xl mx-auto text-center mb-10">{hub.intro}</p>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {guides.map((g) => (
        <a href={`/ru/guides/${g.slug}/`} class="card hover:scale-[1.02] transition flex flex-col">
          <h3 class="mb-3 text-lg">{g.title[locale]}</h3>
          <p class="text-sm text-bakery-navy/70 flex-1">{g.desc[locale]}</p>
          <p class="text-bakery-orange text-sm mt-3 font-semibold">{readLabel} →</p>
        </a>
      ))}
    </div>
  </div>
</section>

<CTASection locale={locale} />
</BaseLayout>
